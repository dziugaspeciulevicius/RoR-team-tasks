# Ruby-on-Rails
💎 Ruby on Rails module team tasks 💎

### TASK5
***Task description:***

Write a static (without the need of using input boxes or database) webpage. Webpage content is your choice.

Project must have:

At least 4 different pages;
All pages can be accessed with the help of navigation menu (which is written in one layouts file);
At least 2 different Controllers (not including application_controller.rb);
Implemented front-end design (for example W3.CSS);
Logically used "before_action";
Code is written using DRY methodology.


### TASK6
***Task description:***

Create webpage with the use of Models - using data input forms in Views, which are stored into database and are displayed in a list.

Webpage content is your choice.

Requirements:

Database system of your choice (Sqlite3 is allowed);
At least 1 data input form, which are stored into database (new/create);
Database's records editing form (edit/update);
Database's records delete functionality;
Database's records list page, in which search form is present by at least 1 table's column value;
Model uses Active Record Validations.


### COURSE PROJECT
***Task description:***

Requirements:

Project must have practical value, like e-shop, blog, forum and etc.;
Database system is used (except Sqlite3);
Users, registration/login - Guest (not logged in), simple user, user with administrator permissions;
Project must comply with RuboCop requirements;
Project logically uses ActiveRecord Associations (Relations);
Project logically uses ActiveRecord Validations;
Project uses any front-end design (for example: W3CSS, Botstrap, Zurb Foundation and etc.);
Project must run successfully without any errors.